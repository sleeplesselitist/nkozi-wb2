# Repo-level example env (copy to .env and set real values)
# Demo controls
REWARD_PER_LISTEN_WLD=0.3
DAILY_CAP_WLD=5

# General RPC + keys (optional global fallbacks)
RPC_URL=https://your-rpc.node
PRIVATE_KEY=0x...
