# Contracts workspace env example (copy to contracts/.env)
# Addresses & deploy config
ARTIST=0xYourArtistAddr
PLATFORM=0xYourTreasuryAddr
RPC_URL=https://your-rpc.node
PRIVATE_KEY=0x...   # private key used for deployment (use safe storage)
